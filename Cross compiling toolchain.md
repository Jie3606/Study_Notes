### 分析Binaries的工具

- **objdump**
  - **功能**：显示二进制文件信息，包括反汇编代码、段信息、符号表等。
  - **使用场景**：用于调试、分析和理解程序的执行流程. 。




- **readelf**
  - **功能**：专门用于显示ELF格式二进制文件的信息，如文件头、段、符号表等。
  - **使用场景**：深入理解ELF文件的内部结构和组织。

- **size**
  - **功能**：显示目标文件、可执行文件或库文件的大小信息，包括文本段、数据段等。
  - **使用场景**：在优化程序大小时，帮助识别不同段的大小。

- **nm**


  - **功能**：列出目标文件或可执行文件中的符号表，包括函数和变量名。
  - **使用场景**：查看程序中定义的符号及其属性。

- **strings**
  - **功能**：从二进制文件中提取可打印的字符串。
  - **使用场景**：在逆向工程或安全分析中查找敏感信息。

### 修改Binaries的工具

- **objcopy**
  - **功能**：复制和转换二进制文件，修改其内容和格式。
  - **使用场景**：提取、转换和简化二进制数据以便进一步分析。

### 减小Binaries大小的工具

- **strip**
  - **功能**：从可执行文件或库文件中移除符号表和调试信息。
  - **使用场景**：发布最终产品时减小文件大小，提高加载速度。

### as (Assembler)
- **功能**：汇编器（Assembler）`as` 的主要作用是将汇编语言代码（也称为组合语言代码，Assembly Language）转换成机器代码（Machine Code）。机器代码是计算机可以直接执行的代码，它是二进制格式的，由CPU的指令集定义。
- **使用场景**：在编译过程中，如果开发者需要直接编写或优化汇编代码，那么这些汇编代码就需要通过汇编器转换成机器代码，然后才能被计算机执行。

### ld (Linker)
- **功能**：链接器（Linker）`ld` 的主要作用是将多个编译后的目标文件（.o 文件）以及库文件（如静态库 .a 或动态库 .so）合并成一个可执行文件或库文件。链接过程中，链接器负责解决函数和变量之间的引用问题，确保所有符号（如函数名和变量名）都被正确解析。
- **使用场景**：在软件开发过程中，程序通常被分割成多个模块进行编译，以提高编译效率和可维护性。链接器负责将这些模块合并成一个完整的可执行文件或库文件。

### ar
- **功能**：`ar` 工具用于创建、修改和从归档文件中提取文件。归档文件（Archive）通常包含多个目标文件（.o 文件），它们可以被链接器用来生成可执行文件或库文件。对于静态库而言，归档文件通常具有 `.a` 扩展名。
- **使用场景**：在创建静态库时，开发者会使用 `ar` 将多个目标文件打包成一个归档文件，并通过 `ranlib` 或其他工具为归档文件创建索引，以便链接器能够快速找到所需的符号。

### ranlib
- **功能**：`ranlib` 工具用于为归档文件（如静态库 .a 文件）生成索引，这个索引记录了归档文件中每个目标文件包含的符号（如函数名和变量名）及其位置。这使得链接器在链接程序时能够更快地找到所需的符号。
- **使用场景**：虽然 `ar` 工具可以创建归档文件，但它不会自动为归档文件生成索引。因此，在创建静态库后，通常需要使用 `ranlib` 来为归档文件生成索引，以提高链接效率。然而，现代版本的 `ar` 工具（如GNU Binutils中的`ar`）通常内置了生成索引的功能，因此在使用这些工具时可能不需要显式调用 `ranlib`。

### make menuconfig
- 1、 make defconfig
　　首先通过make xxx_defconfig，生成最开始的.config，相当于把 XXX_defconfig 文件复制为 .config 文件，其中 defconfig 是最小的 config 项，kernel编译会根据 .config 文件去编译驱动情况，加载过改指令后，后面的 make  menuconfig 就会基于现在的 .config 去配置 config ；

- 2、make  menuconfig
　　make  menuconfig 的作用类似于 make  config ，就是基于界面去配置 config 文件，make  config 的作用是加载 “ .config ” 作为默认的配置，配置它就是相当于用图形化界面配置 .config 文件；

- 3、make  savedefconfig
　　执行 make saveconfig 作用是通过执行.config 生成最小的 defconfig 文件；

- 4、make  olddefconfig
　　通过make oldconfig将刚增加的config项的.config做依赖检查重新生成新的.config文件，且新生成的.config和以前的不同是，将旧的.config重命名为.config.old文件。

### GPL License

GPL（GNU General Public License）是GNU项目发布的一种自由软件许可证，它保证了软件的自由使用和修改，同时要求任何基于GPL许可的代码在分发时也必须保持开源和免费。GPL许可证的核心原则是代码的共享和创新，它促进了开源软件社区的发展和繁荣

- modinfo: 顯示指定kernel module的資訊
- lsmod: 列出當前已加載的kernel modules
- modprobe: 用於自動加載、卸載和查詢kernel module。根據module之間的依賴關係自動加載所需的kernel modules。
- insmod: 手動加載kernel module
- rmmod： 卸載指定kernel module