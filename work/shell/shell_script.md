```shell
$0 : 脚本名
$0-$9: 脚本的参数
$@ : 所有参数
$# : 参数个数
$? : 前一个命令的返回值
$$ : 当前脚本的识别码
!! : 完整的上一条命令(包括参数)
$_ : 上一条命令的最后一个参数
```

```Makefile
hey : one two
    echo $@ 输出hey
    echo $? 输出比目标更新的所有先决条件
    echo $n 输出所有先决条件
```